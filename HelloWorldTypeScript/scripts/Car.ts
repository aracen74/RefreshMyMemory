module car {
    var engine: string;
    var price: number;
    console.log(engine.substring(1, 4));

    // how do I create a class
    // how do I add properties
    // can I inherit from a class or interface

}